info:
  name: Get Product By Id
  type: http
  seq: 2

http:
  method: GET
  url: http://127.0.0.1.:80/rbn/{{namespace}}/products/2
  headers:
    - name: X-API-KEY
      value: "{{namespace}}-secret-key"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This request retrieves a list of users from the JSONPlaceholder API.
